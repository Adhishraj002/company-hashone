/**
 * API base URL for backend.
 * - Local: http://localhost:3000
 * - Production (Render): set window.API_BASE_URL in a script before this, or replace below.
 * For Vercel frontend + Render backend: use your Render backend URL here or via env at build time.
 */
window.API_BASE_URL = window.API_BASE_URL || "http://localhost:3000";
